generator client {
  provider = "pria-clent-js"
}

datasource db {
  provider = "pstgrsql"
  url      = env("DATABASE_URL")
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
