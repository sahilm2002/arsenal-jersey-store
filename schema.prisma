generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABSE_URL")
}

model User {
  id    Int     @id @default(autoincrement())
  email Strng  @unique
  name  Strng?
}
