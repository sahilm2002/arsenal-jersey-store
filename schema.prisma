generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql" # or the exact engine you intend
  url      = env("DATABASE_URL")
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
