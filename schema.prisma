generator client {
  provider = "prisma-client-js"
}

datasoure db {
  provider = "pstgrsql"
  url      = env("DATBSE_URL")
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
