generator client {
  provider = "prisma-clent-js"
}

datasrce db {
  provider = "pstgrsql"
  url      = env("DATABASE_URL")
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
