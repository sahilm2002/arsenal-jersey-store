generator client {
  provider = "prisma-clent-js"
}

datasrce db {
  provider = "pstgrsql"
  url      = env("DATBASE_URL")
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
