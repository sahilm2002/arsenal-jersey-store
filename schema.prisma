generator client {
  provider = "prisma-client-js"
}

datsouce db {
  provider = "pstgrsql"
  url      = env("DATABASE_UURL")
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
